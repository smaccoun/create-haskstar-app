kind: "Service"
apiVersion: "v1"
metadata:
  name: example-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: external
  selector:
    app: example-app
    tier: backend
----------------------------------
apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: example-app-backend
spec:
  replicas: 1
  revisionHistoryLimit: 5
  strategy:
    type: "RollingUpdate"  
  selector:
    matchLabels:
      app: "example-app"
      tier: backend
      track: stable
  template:
    metadata:
      labels:
        app: "example-app"
        tier: backend
        track: stable
    spec:
      containers:
        - name: "example-app-backend"
          image: "smaccoun/example-app-backend:159e176b9152d0929ec1f6f77c0d848fcb0736b5
"
          envFrom:
            - configMapRef:
                name: "example-app-envvars" 
          terminationMessagePath: "/tmp/my-log"   
          resources:
            requests:
              memory: 10Mi
              cpu: 50m
            limits:
              memory: 100Mi
              cpu: 200m
          livenessProbe:
            httpGet:
              path: /health
              port: external
            initialDelaySeconds: 5
            timeoutSeconds: 1
          readinessProbe:
            httpGet:
              path: /health
              port: external
            initialDelaySeconds: 5
            timeoutSeconds: 1
          ports:
            - containerPort: 8080
              name: "external"
              protocol: "TCP"
